import React from "react";
import styles from "./Presentation.module.css";

export default function Presentation() {
    return (
        <section>
            <h1 className={styles.title}>Événements sauvegardés</h1>
        </section>
    );
}
